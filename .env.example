# IntroFlow .env.example (TEMPLATE ONLY)
# - Do NOT put real secrets here.
# - Copy to .env locally if needed (never commit .env).
# - Keys below are required by introflow.config.settings (Step 34).

DATABASE_URL=
APP_ENV=
INTROFLOW_LOG_JSON=